/** Browser-wide preference keys owned by DSH Inline Comments. */

/** Fresh installations expose the feature until the user disables it. */
export const DEFAULT_INLINE_COMMENTS_ENABLED = true

/** Local persistence key for the browser-wide feature toggle. */
export const INLINE_COMMENTS_ENABLED_STORAGE_KEY = 'dsh.inline-comments.enabled'
